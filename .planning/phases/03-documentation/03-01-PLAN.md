---
phase: 03-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - DOC-01
must_haves:
  truths:
    - "README opens with a concrete value proposition and terminal demo before the fold"
    - "A developer can decide yes/no on the tool within 60 seconds of opening README"
    - "Installation section is copy-paste complete — clone, deps, vault, model, hooks — no external knowledge needed"
    - "Cost transparency is explicit — $0.002/session, ~$1/month at 5 sessions/day"
    - "1.1GB model download is prominently warned before the user encounters it"
  artifacts:
    - path: "README.md"
      provides: "Complete open-source README for new users"
      contains: "Persistent engineering memory for Claude Code"
  key_links:
    - from: "README.md"
      to: "examples/hooks.json"
      via: "reference in installation section"
      pattern: "examples/hooks\\.json"
---

<objective>
Rewrite README.md for an open-source audience. The current README is 15.4KB and comprehensive but poorly structured for a new user's decision process — features come before the value proposition demo, installation is incomplete (missing env var setup and 1.1GB warning), and the tone uses marketing language.

Purpose: A new user must be able to understand what claude-memo does, decide if they want it, and install it — all from README alone (DOC-01).
Output: Rewritten README.md (~400-500 lines)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-documentation/03-CONTEXT.md
@.planning/phases/03-documentation/03-RESEARCH.md
@README.md
@pyproject.toml
@SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite README.md for open-source audience</name>
  <files>README.md</files>
  <read_first>
    README.md
    SKILL.md
    pyproject.toml
    .planning/phases/03-documentation/03-RESEARCH.md
  </read_first>
  <action>
Rewrite README.md following this exact structure (per D-01):

**1. Title + tagline (no emoji in title)**
```
# Memo

Persistent engineering memory for Claude Code. Automatic capture, semantic search, cross-project intelligence.
```

**2. Badges (per D-03)**
```markdown
![CI](https://github.com/USERNAME/claude-memo/actions/workflows/ci.yml/badge.svg)
![Python 3.10+](https://img.shields.io/badge/python-3.10%2B-blue)
![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)
```
Use `USERNAME` placeholder — Phase 4 fills in real URL. CI badge is speculative (activates after Phase 4).

**3. Value proposition (2-3 sentences, per D-02 developer-to-developer tone)**
Something like: "Claude Code forgets everything between sessions. Memo builds a persistent knowledge vault — every architecture decision, debug solution, and code pattern is captured automatically and searchable by meaning. One vault across all projects."

**4. Before/after terminal demo (per D-01, D-04)**
Show a concrete terminal demo. "Before" block: user re-solves same problem. "After" block: Claude already knows the fix because it loaded from vault at session start. Use realistic but generic examples (no real project names per SEC-03). Format as code blocks with comments showing what happens.

Example structure:
```
# Without Memo — session 14, same CORS bug again
$ claude
> How do I fix CORS with FastAPI and a React frontend?
  [20 minutes of debugging...]

# With Memo — Claude loads the fix at session start
$ claude
  [SessionStart hook loads relevant notes]
> How do I fix CORS with FastAPI and a React frontend?
  I found your previous solution in the vault:
  decisions/2026-03-15-fastapi-cors-config.md
  ...
```

**5. Features list (bullet points, scannable)**
Reuse existing features from current README lines 25-40. Keep the structure: bold feature name, dash, one-line description. Remove marketing language like "Every architecture decision, every debug breakthrough" — replace with concrete specifics (per D-02).

**6. Requirements section (before install, prominent)**
List upfront:
- Python 3.10+
- ~1.1GB disk for embedding model (one-time download) — **make this a visible callout/blockquote**
- ~1.5GB RAM for embedding model
- Claude Code (for hooks integration)
- ANTHROPIC_API_KEY (optional, for auto-memo via Haiku — ~$0.002/session)
- Obsidian (optional, for graph visualization)

**7. Installation (per D-05, D-06, D-07 — copy-paste complete, 5 steps)**
```bash
# 1. Clone
git clone https://github.com/USERNAME/claude-memo.git ~/.claude/skills/memo-skill

# 2. Install dependencies
cd ~/.claude/skills/memo-skill
pip install -r requirements.txt
# Or with uv (faster): uv pip install -r requirements.txt

# 3. Initialize vault
bash scripts/init_vault.sh ~/memo-vault

# 4. Download embedding model (~1.1GB, one-time, takes 2-5 minutes)
python3 scripts/memo_engine.py warm-up

# 5. Set up automation (cron reindex, git backup)
bash scripts/setup_automation.sh ~/memo-vault
```

Then env vars in shell profile:
```bash
export MEMO_VAULT_PATH=~/memo-vault
export ANTHROPIC_API_KEY=your-key-here  # Required for auto-memo
```

Then hooks — show inline AND reference `examples/hooks.json`:
"Copy `examples/hooks.json` into your `~/.claude/settings.json`, or add manually:"
Show the hooks JSON using the `$MEMO_VAULT_PATH` form from SKILL.md (not hardcoded `~/memo-vault`):
- SessionEnd command: `bash ~/.claude/skills/memo-skill/scripts/auto_memo_hook.sh $MEMO_VAULT_PATH`
- All other hooks: same as SKILL.md lines 84-118

**8. Usage section**
Keep the commands table from current README (lines 119-131). Add 2-3 examples with terminal output (per D-04):
- `/memo find` example showing search results
- `/memo` save example showing note creation confirmation
- Terminal query example: `python3 scripts/memo_engine.py query "how did I solve the CORS issue" --vault ~/memo-vault`

**9. How it works (short, optional per discretion)**
Keep the ASCII architecture diagram from current README (lines 134-158). Keep the 3-layer explanation. Keep it concise.

**10. Auto-capture table**
Reuse from current README (lines 219-226).

**11. Cost transparency (dedicated section, per DOC-01)**
Combine scattered cost info into one clear section:
- Embedding model: free, runs locally
- Auto-memo classification: ~$0.002/session via Claude Haiku
- Terminal query: ~$0.001/query via Haiku
- At 5 sessions/day: ~$1/month
- Manual `/memo` only (no API): completely free

**12. Note format**
Keep from current README (lines 169-213).

**13. Vault structure**
Keep from current README (lines 233-251).

**14. Configuration (env vars table)**
Keep from current README (lines 276-288).

**15. FAQ**
Keep from current README (lines 290-319). Add FAQ about 1.1GB download.

**16. Contributing**
Brief pointer: "See [CONTRIBUTING.md](CONTRIBUTING.md) for development setup, code style, and PR guidelines."

**17. License**
"MIT — see [LICENSE](LICENSE)"

**Tone rules (per D-02):** No marketing language. No "every architecture decision, every debug breakthrough". Concrete, direct, developer-to-developer. Show, don't tell.

**Sensitive data rules (per SEC-01 through SEC-03):** No real project names. Use generic examples (my-saas-project, my-api, etc.). No personal names. No API keys.
  </action>
  <verify>
    <automated>python3 -c "
import re
with open('README.md') as f:
    content = f.read()
# Check structure exists
checks = [
    ('# Memo' in content, 'Title present'),
    ('shields.io' in content, 'Badges present'),
    ('pip install -r requirements.txt' in content, 'Install command present'),
    ('MEMO_VAULT_PATH' in content, 'Env var documented'),
    ('ANTHROPIC_API_KEY' in content, 'API key documented'),
    ('1.1GB' in content or '1.1 GB' in content, '1.1GB warning present'),
    ('examples/hooks.json' in content, 'Reference to examples/hooks.json'),
    ('0.002' in content, 'Cost transparency present'),
    ('CONTRIBUTING.md' in content, 'Link to CONTRIBUTING'),
    ('LICENSE' in content, 'License reference'),
]
for ok, msg in checks:
    status = 'PASS' if ok else 'FAIL'
    print(f'{status}: {msg}')
assert all(ok for ok, _ in checks), 'Some checks failed'
print('All README checks passed')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - README.md starts with `# Memo` (not `# 🧠 Memo`)
    - README.md contains `shields.io` badge URLs (at least 2 badges)
    - README.md contains `pip install -r requirements.txt` in installation section
    - README.md contains `uv pip install` as alternative (per D-06)
    - README.md contains `MEMO_VAULT_PATH` env var setup
    - README.md contains `ANTHROPIC_API_KEY` env var setup
    - README.md contains `1.1GB` or `1.1 GB` warning prominently (before install steps or at start of install)
    - README.md contains `examples/hooks.json` reference
    - README.md contains `$MEMO_VAULT_PATH` in SessionEnd hook command (not hardcoded ~/memo-vault)
    - README.md contains cost section with `$0.002` per session
    - README.md contains `## Cost` or `## Cost transparency` or similar dedicated cost section
    - README.md contains link to `CONTRIBUTING.md`
    - README.md contains no emoji in the H1 title
    - README.md does not contain `JobHunter`, `Finn.no`, `Webcruiter`, `Jobbnorge`, `engineering-brain`
  </acceptance_criteria>
  <done>README.md is rewritten with value-prop-first structure, copy-paste complete installation, cost transparency section, and developer-to-developer tone. A new user can evaluate and install claude-memo from README alone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README content -> user terminal | Users copy-paste commands from README into their shell |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | README.md | mitigate | Verify no personal data, API keys, or real project names in README content. Automated check in verify step scans for known sensitive strings (JobHunter, Finn.no, engineering-brain). |
| T-03-02 | Tampering | README install commands | accept | Install commands use HTTPS clone URL and pip from requirements.txt. Standard open-source install pattern — user verifies repo before cloning. Low risk for a docs-only phase. |
</threat_model>

<verification>
1. README.md exists and is >200 lines (comprehensive rewrite, not stub)
2. README opens with `# Memo` title, badges, then value proposition before any code
3. Installation section has all 5 steps + env var setup + hooks config
4. No sensitive data (personal names, real project names, API keys)
5. `examples/hooks.json` is referenced in installation section
</verification>

<success_criteria>
A developer unfamiliar with claude-memo can read the README, understand the value proposition within 60 seconds, and follow the installation steps to a working setup without needing any external documentation.
</success_criteria>

<output>
After completion, create `.planning/phases/03-documentation/03-01-SUMMARY.md`
</output>
