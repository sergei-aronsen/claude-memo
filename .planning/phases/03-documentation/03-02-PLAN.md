---
phase: 03-documentation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/hooks.json
  - CONTRIBUTING.md
  - CHANGELOG.md
  - pyproject.toml
autonomous: true
requirements:
  - DOC-02
  - DOC-03
  - DOC-04
must_haves:
  truths:
    - "examples/hooks.json is valid JSON and contains all 4 Claude Code hooks (SessionStart, SessionEnd, PreCompact, Stop)"
    - "CONTRIBUTING.md has dev setup, code style, PR process, and issue guidelines sections"
    - "CHANGELOG.md has a v1.0.0 entry in Keep a Changelog format"
    - "pyproject.toml version matches CHANGELOG version (1.0.0)"
  artifacts:
    - path: "examples/hooks.json"
      provides: "Copy-paste ready hooks config for Claude Code"
      contains: "SessionStart"
    - path: "CONTRIBUTING.md"
      provides: "Contributor guidelines"
      contains: "Development Setup"
    - path: "CHANGELOG.md"
      provides: "Project changelog"
      contains: "[1.0.0]"
  key_links:
    - from: "CHANGELOG.md"
      to: "pyproject.toml"
      via: "version number match"
      pattern: "1\\.0\\.0"
---

<objective>
Create the three new files required by DOC-02, DOC-03, DOC-04: examples/hooks.json, CONTRIBUTING.md, and CHANGELOG.md. Also bump pyproject.toml version to 1.0.0 to match the CHANGELOG entry (resolving the 0.1.0 vs 1.0.0 inconsistency flagged in RESEARCH.md).

Purpose: Complete the documentation deliverables beyond README — contributor guide, changelog, and a copy-paste hooks config file.
Output: 3 new files + 1 version bump in pyproject.toml
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-documentation/03-CONTEXT.md
@.planning/phases/03-documentation/03-RESEARCH.md
@pyproject.toml
@SKILL.md
</context>

<interfaces>
<!-- From SKILL.md lines 84-118 — canonical hooks config with $MEMO_VAULT_PATH -->
<!-- From pyproject.toml — current version = "0.1.0", needs bump to "1.0.0" -->
<!-- From RESEARCH.md — Keep a Changelog format, CONTRIBUTING.md skeleton -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create examples/hooks.json</name>
  <files>examples/hooks.json</files>
  <read_first>
    SKILL.md
    README.md
  </read_first>
  <action>
Create the `examples/` directory and `examples/hooks.json` file (per D-08, D-09).

The file must contain exactly this JSON (from SKILL.md lines 84-118, which is the canonical production form using `$MEMO_VAULT_PATH`):

```json
{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup",
        "hooks": [
          {
            "type": "command",
            "command": "python3 ~/.claude/skills/memo-skill/scripts/session_context.py"
          }
        ]
      }
    ],
    "SessionEnd": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "bash ~/.claude/skills/memo-skill/scripts/auto_memo_hook.sh $MEMO_VAULT_PATH"
          }
        ]
      }
    ],
    "PreCompact": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "python3 ~/.claude/skills/memo-skill/scripts/pre_compact_save.py"
          }
        ]
      }
    ],
    "Stop": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "python3 ~/.claude/skills/memo-skill/scripts/should_suggest_memo.py",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
```

Key: SessionEnd uses `$MEMO_VAULT_PATH` (env var form), NOT hardcoded `~/memo-vault`. This is the correct production form per RESEARCH.md.

Do NOT create any other files in examples/ (per D-09).
  </action>
  <verify>
    <automated>python3 -m json.tool /Users/sergeiarutiunian/Projects/claude-memo/examples/hooks.json > /dev/null && python3 -c "
import json
with open('examples/hooks.json') as f:
    data = json.load(f)
hooks = data['hooks']
assert 'SessionStart' in hooks, 'Missing SessionStart'
assert 'SessionEnd' in hooks, 'Missing SessionEnd'
assert 'PreCompact' in hooks, 'Missing PreCompact'
assert 'Stop' in hooks, 'Missing Stop'
assert 'MEMO_VAULT_PATH' in hooks['SessionEnd'][0]['hooks'][0]['command'], 'SessionEnd must use MEMO_VAULT_PATH env var'
print('All hooks.json checks passed')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - File `examples/hooks.json` exists
    - `python3 -m json.tool examples/hooks.json` exits 0 (valid JSON)
    - File contains keys: `SessionStart`, `SessionEnd`, `PreCompact`, `Stop`
    - SessionEnd command string contains `$MEMO_VAULT_PATH` (not hardcoded `~/memo-vault`)
    - SessionStart command contains `session_context.py`
    - Stop hook has `"timeout": 5`
    - No other files exist in `examples/` directory (per D-09)
  </acceptance_criteria>
  <done>examples/hooks.json exists with all 4 hooks in production form, valid JSON, ready for users to copy into their Claude Code settings.</done>
</task>

<task type="auto">
  <name>Task 2: Create CONTRIBUTING.md</name>
  <files>CONTRIBUTING.md</files>
  <read_first>
    pyproject.toml
    .planning/phases/03-documentation/03-RESEARCH.md
  </read_first>
  <action>
Create CONTRIBUTING.md (per D-10, D-11). Standard depth, no architecture guide.

File must contain these exact sections with this content:

```markdown
# Contributing to claude-memo

## Development Setup

```bash
git clone https://github.com/USERNAME/claude-memo.git
cd claude-memo
pip install -r requirements.txt
```

## Code Style

This project uses [ruff](https://docs.astral.sh/ruff/) for linting and formatting, and [mypy](https://mypy-lang.org/) for type checking.

```bash
# Lint
ruff check scripts/

# Format
ruff format scripts/

# Type check
mypy scripts/memo_engine.py scripts/auto_memo.py
```

All checks must pass before submitting a PR.

## Tests

No automated test suite in v1.0.0. Testing is done manually against a local vault:

```bash
# Initialize a test vault
bash scripts/init_vault.sh /tmp/test-vault

# Run warm-up
python3 scripts/memo_engine.py warm-up

# Test search
python3 scripts/memo_engine.py search "test query" --vault /tmp/test-vault
```

## Pull Requests

1. Fork the repo and create a branch: `feature/your-feature` or `fix/your-fix`
2. Make your changes — keep commits focused and descriptive
3. Ensure `ruff check scripts/` and `ruff format --check scripts/` pass
4. Ensure `mypy scripts/memo_engine.py scripts/auto_memo.py` passes
5. Open a PR with a clear description of what changed and why

## Issues

**Bug reports** — include: Python version, OS, full error message, and steps to reproduce.

**Feature requests** — describe the problem it solves, not just the solution you want.

## Design Principles

- The Obsidian vault (markdown files) is the source of truth. The index is always derived and rebuildable.
- No external services required (except optional Haiku for auto-memo).
- Everything must work for a single developer with 5 notes or 5,000.
```

Use `USERNAME` placeholder for GitHub URL (Phase 4 fills in real URL).
Do NOT include architecture guide or deep technical docs (per D-11).
  </action>
  <verify>
    <automated>python3 -c "
with open('CONTRIBUTING.md') as f:
    content = f.read()
checks = [
    ('## Development Setup' in content, 'Dev setup section'),
    ('## Code Style' in content, 'Code style section'),
    ('ruff check' in content, 'Ruff command'),
    ('mypy' in content, 'Mypy command'),
    ('## Pull Requests' in content, 'PR process section'),
    ('## Issues' in content, 'Issue guidelines section'),
    ('pip install -r requirements.txt' in content, 'Install command'),
]
for ok, msg in checks:
    status = 'PASS' if ok else 'FAIL'
    print(f'{status}: {msg}')
assert all(ok for ok, _ in checks), 'Some checks failed'
print('All CONTRIBUTING.md checks passed')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - File `CONTRIBUTING.md` exists
    - Contains `## Development Setup` section with `pip install -r requirements.txt`
    - Contains `## Code Style` section with `ruff check scripts/` and `mypy` commands
    - Contains `## Pull Requests` section with branch naming convention (`feature/` or `fix/`)
    - Contains `## Issues` section with bug report and feature request guidance
    - Does NOT contain `## Architecture` section (per D-11)
    - Does not contain any real project names, personal names, or API keys
  </acceptance_criteria>
  <done>CONTRIBUTING.md exists with dev setup, code style, PR process, and issue guidelines. Standard depth per D-10, no architecture docs per D-11.</done>
</task>

<task type="auto">
  <name>Task 3: Create CHANGELOG.md and bump pyproject.toml to v1.0.0</name>
  <files>CHANGELOG.md, pyproject.toml</files>
  <read_first>
    pyproject.toml
    .planning/REQUIREMENTS.md
    .planning/phases/03-documentation/03-RESEARCH.md
  </read_first>
  <action>
**Part A: Create CHANGELOG.md** (per D-12)

Use Keep a Changelog 1.1.0 format (keepachangelog.com). Content:

```markdown
# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [1.0.0] - 2026-04-13

### Added

- Persistent engineering memory vault with Obsidian-native markdown notes
- Semantic search via `intfloat/multilingual-e5-large` (1024 dims, 50+ languages)
- Keyword search via SQLite FTS5 index
- Automatic capture: SessionEnd hook classifies conversations via Claude Haiku
- Mid-session save: PreCompact hook preserves context before compression
- Smart context loading: SessionStart hook loads relevant notes into new sessions
- Decision nudge: Stop hook detects decision-making language and suggests `/memo`
- 10 slash commands: save, find, query, list, project, dedup, lint, stats, link, reindex
- Daily log compilation via cron (compile_logs.py)
- Vault health checks: broken links, orphans, empty notes, missing frontmatter
- Deduplication via semantic similarity threshold
- Race-safe writes via fcntl.flock file locking
- Git-backed vault with automated push via cron
- MCP server for Claude Desktop and Cursor integration
- Copy-paste hooks config in examples/hooks.json

### Changed

- Default vault path standardized to `~/memo-vault`
- All dependencies pinned in requirements.txt for reproducible installs

### Fixed

- Scripts now exit with explicit error when MEMO_VAULT_PATH is not set (instead of silent failure)
- Ruff and mypy pass cleanly on all modules

[1.0.0]: https://github.com/USERNAME/claude-memo/releases/tag/v1.0.0
```

The date `2026-04-13` matches the current date. Use `USERNAME` placeholder in the URL.

**Part B: Bump pyproject.toml version**

In `pyproject.toml`, change line `version = "0.1.0"` to `version = "1.0.0"`.

This resolves the version inconsistency flagged in RESEARCH.md (Pitfall 5): pyproject.toml said 0.1.0 but CONTEXT.md D-12 requires a v1.0.0 CHANGELOG entry.
  </action>
  <verify>
    <automated>python3 -c "
# Check CHANGELOG
with open('CHANGELOG.md') as f:
    cl = f.read()
checks = [
    ('Keep a Changelog' in cl, 'Keep a Changelog reference'),
    ('Semantic Versioning' in cl, 'SemVer reference'),
    ('[1.0.0]' in cl, 'v1.0.0 entry'),
    ('### Added' in cl, 'Added section'),
    ('2026-04-13' in cl, 'Correct date'),
]

# Check pyproject.toml version
with open('pyproject.toml') as f:
    pt = f.read()
checks.append(('version = \"1.0.0\"' in pt, 'pyproject.toml version bumped to 1.0.0'))

for ok, msg in checks:
    status = 'PASS' if ok else 'FAIL'
    print(f'{status}: {msg}')
assert all(ok for ok, _ in checks), 'Some checks failed'
print('All CHANGELOG + version checks passed')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - File `CHANGELOG.md` exists
    - Contains `[Keep a Changelog](https://keepachangelog.com/en/1.1.0/)` link
    - Contains `[Semantic Versioning](https://semver.org/spec/v2.0.0.html)` link
    - Contains `## [1.0.0] - 2026-04-13` header
    - Contains `### Added` section listing features
    - Contains footer link `[1.0.0]: https://github.com/USERNAME/claude-memo/releases/tag/v1.0.0`
    - `pyproject.toml` contains `version = "1.0.0"` (not `0.1.0`)
    - CHANGELOG does not contain real project names, personal names, or API keys
  </acceptance_criteria>
  <done>CHANGELOG.md exists with v1.0.0 entry in Keep a Changelog format. pyproject.toml version bumped to 1.0.0 to match.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| examples/hooks.json -> user shell | Users copy hook commands that execute scripts |
| CONTRIBUTING.md -> contributor workflow | Contributors follow documented commands |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Information Disclosure | CONTRIBUTING.md, CHANGELOG.md | mitigate | Verify no personal data, API keys, or real project names in new files. acceptance_criteria checks for absence of known sensitive strings. |
| T-03-04 | Tampering | examples/hooks.json | accept | Hooks config runs local scripts from user's own clone. No remote code execution. Standard pattern for Claude Code skills. |
</threat_model>

<verification>
1. `python3 -m json.tool examples/hooks.json` exits 0
2. `examples/hooks.json` contains all 4 hook types
3. `CONTRIBUTING.md` contains all required sections (dev setup, code style, PRs, issues)
4. `CHANGELOG.md` follows Keep a Changelog format with v1.0.0 entry
5. `pyproject.toml` version is `"1.0.0"`
6. No sensitive data in any created file
</verification>

<success_criteria>
All three documentation deliverables exist (examples/hooks.json, CONTRIBUTING.md, CHANGELOG.md), are correctly formatted, and pyproject.toml version matches the CHANGELOG entry at 1.0.0.
</success_criteria>

<output>
After completion, create `.planning/phases/03-documentation/03-02-SUMMARY.md`
</output>
