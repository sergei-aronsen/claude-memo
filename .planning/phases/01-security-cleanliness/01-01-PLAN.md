---
phase: 01-security-cleanliness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/auto_memo_hook.sh
  - scripts/auto_memo.py
  - scripts/compile_logs.py
  - scripts/init_vault.sh
  - scripts/memo_engine.py
  - scripts/memo_mcp_server.py
  - scripts/memo_utils.py
  - scripts/pre_compact_save.py
  - scripts/save_raw_log.py
  - scripts/session_context.py
  - scripts/setup_automation.sh
  - scripts/should_suggest_memo.py
  - references/ARCHITECTURE.md
  - references/AUTO_MEMO_SETUP.md
  - .gitignore
  - LICENSE
  - README.md
  - requirements.txt
  - SKILL.md
autonomous: true
requirements:
  - SEC-01
  - SEC-02
  - SEC-03
  - SEC-04
  - SEC-05

must_haves:
  truths:
    - "All source files from ~/Downloads/memo-skill/ exist in ~/Projects/claude-memo/ with directory structure preserved"
    - "The .planning/ directory is untouched — no files overwritten"
    - "The installed copy at ~/.claude/skills/memo-skill/ was never used as a source"
  artifacts:
    - path: "scripts/memo_engine.py"
      provides: "Core memory engine — largest source file (37KB)"
    - path: "scripts/memo_mcp_server.py"
      provides: "MCP server entry point"
    - path: "references/ARCHITECTURE.md"
      provides: "Architecture reference document"
    - path: "SKILL.md"
      provides: "Claude Code skill index"
    - path: "README.md"
      provides: "Project README (pre-sanitization)"
  key_links:
    - from: "~/Downloads/memo-skill/"
      to: "~/Projects/claude-memo/"
      via: "cp commands for named items only"
      pattern: "cp -r.*memo-skill"
---

<objective>
Copy all source files from the clean zip (`~/Downloads/memo-skill/`) into the target repo (`~/Projects/claude-memo/`), preserving directory structure. This is the foundation step — no sanitization yet, just getting the files in place.

Purpose: All subsequent sanitization tasks operate on files inside the repo. The copy must happen first and must come from the right source.
Output: `scripts/`, `references/`, and all root files present in repo root. `.planning/` directory untouched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-security-cleanliness/01-CONTEXT.md
@.planning/phases/01-security-cleanliness/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copy source files from clean zip into repo</name>
  <read_first>
    - ~/Downloads/memo-skill/ (directory listing — confirm structure before copying)
    - ~/Projects/claude-memo/ (directory listing — confirm .planning/ exists and nothing else critical)
    - .planning/phases/01-security-cleanliness/01-CONTEXT.md (decisions D-01, D-02)
  </read_first>
  <files>
    scripts/auto_memo_hook.sh
    scripts/auto_memo.py
    scripts/compile_logs.py
    scripts/init_vault.sh
    scripts/memo_engine.py
    scripts/memo_mcp_server.py
    scripts/memo_utils.py
    scripts/pre_compact_save.py
    scripts/save_raw_log.py
    scripts/session_context.py
    scripts/setup_automation.sh
    scripts/should_suggest_memo.py
    references/ARCHITECTURE.md
    references/AUTO_MEMO_SETUP.md
    .gitignore
    LICENSE
    README.md
    requirements.txt
    SKILL.md
  </files>
  <action>
    Copy files from `~/Downloads/memo-skill/` into `~/Projects/claude-memo/` using named items only — do NOT use `cp -r ~/Downloads/memo-skill/. ~/Projects/claude-memo/` (risks overwriting .planning/).

    Run these commands in order:

    ```bash
    # Copy directories
    cp -r ~/Downloads/memo-skill/scripts ~/Projects/claude-memo/
    cp -r ~/Downloads/memo-skill/references ~/Projects/claude-memo/

    # Copy root files individually (explicit list — no wildcards that could catch unexpected files)
    cp ~/Downloads/memo-skill/README.md ~/Projects/claude-memo/README.md
    cp ~/Downloads/memo-skill/SKILL.md ~/Projects/claude-memo/SKILL.md
    cp ~/Downloads/memo-skill/requirements.txt ~/Projects/claude-memo/requirements.txt
    cp ~/Downloads/memo-skill/LICENSE ~/Projects/claude-memo/LICENSE
    cp ~/Downloads/memo-skill/.gitignore ~/Projects/claude-memo/.gitignore
    ```

    CRITICAL: Never copy from `~/.claude/skills/memo-skill/` — that installed copy is confirmed to contain personal project names (`jobhunter`, `digital-planet` in memo_mcp_server.py line 147). Source MUST be `~/Downloads/memo-skill/` only (per D-01, D-02).

    After copying, verify .planning/ is intact:
    ```bash
    ls ~/Projects/claude-memo/.planning/
    ```
    Must show: STATE.md, ROADMAP.md, REQUIREMENTS.md, phases/ — if any are missing, STOP.
  </action>
  <verify>
    <automated>
      # All scripts copied
      ls ~/Projects/claude-memo/scripts/ | wc -l
      # Expected: 12 files

      # All references copied
      ls ~/Projects/claude-memo/references/
      # Expected: ARCHITECTURE.md AUTO_MEMO_SETUP.md

      # Root files present
      ls ~/Projects/claude-memo/README.md ~/Projects/claude-memo/SKILL.md ~/Projects/claude-memo/requirements.txt ~/Projects/claude-memo/LICENSE ~/Projects/claude-memo/.gitignore
      # Expected: all 5 files listed without error

      # .planning/ untouched
      ls ~/Projects/claude-memo/.planning/STATE.md
      # Expected: file exists
    </automated>
  </verify>
  <acceptance_criteria>
    - `ls ~/Projects/claude-memo/scripts/ | wc -l` returns 12
    - `ls ~/Projects/claude-memo/references/` shows exactly `ARCHITECTURE.md` and `AUTO_MEMO_SETUP.md`
    - All 5 root files exist: README.md, SKILL.md, requirements.txt, LICENSE, .gitignore
    - `ls ~/Projects/claude-memo/.planning/STATE.md` exits 0 (file present, not overwritten)
    - `ls ~/Projects/claude-memo/.planning/phases/` shows `01-security-cleanliness/` directory
  </acceptance_criteria>
  <done>All source files from ~/Downloads/memo-skill/ copied into repo root with directory structure intact. .planning/ directory unchanged. No files sourced from installed copy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source selection | Two possible sources exist; only the clean zip is safe. Using the wrong source brings contaminated data into git history permanently. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | cp source selection | mitigate | Action explicitly names `~/Downloads/memo-skill/` as source and prohibits `~/.claude/skills/memo-skill/` with rationale |
| T-01-02 | Tampering | .planning/ overwrite | mitigate | Copy uses named items only (never wildcard `cp -r source/. target/`); verification step confirms .planning/ integrity |
</threat_model>

<verification>
After task completes:
1. `ls ~/Projects/claude-memo/scripts/` — 12 Python/shell files
2. `ls ~/Projects/claude-memo/references/` — 2 markdown files
3. `ls ~/Projects/claude-memo/.planning/STATE.md` — exits 0
4. `ls ~/Projects/claude-memo/.planning/phases/01-security-cleanliness/` — shows PLAN files and CONTEXT.md
</verification>

<success_criteria>
All 19 source files present in repo. .planning/ directory untouched. Ready for sanitization in Plan 02 and Plan 03.
</success_criteria>

<output>
After completion, create `.planning/phases/01-security-cleanliness/01-01-SUMMARY.md`
</output>
