---
phase: 01-security-cleanliness
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - README.md
autonomous: false
requirements:
  - SEC-03

must_haves:
  truths:
    - "README.md contains no occurrences of JobHunter, Finn.no, Webcruiter, or Jobbnorge"
    - "The structure and educational value of README examples is preserved — only the company/project names changed"
    - "A human has visually reviewed the examples section and confirmed no subtle personal references remain"
  artifacts:
    - path: "README.md"
      provides: "Sanitized README with generic project name examples"
      contains: "my-saas-project"
  key_links:
    - from: "README.md examples section"
      to: "generic project names"
      via: "sed replacement"
      pattern: "my-saas-project|generic-jobs-api|careers-api|jobboard-api"
---

<objective>
Sanitize the README.md example section to remove real company and project names, then pause for a human visual review to confirm no subtle personal references were missed.

Purpose: Automated sed handles the known names. The human checkpoint catches anything subtle that pattern-matching misses (per D-08).
Output: README.md with generic example names. Human confirmation that examples look clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-security-cleanliness/01-CONTEXT.md
@.planning/phases/01-security-cleanliness/01-RESEARCH.md
@.planning/phases/01-security-cleanliness/01-01-SUMMARY.md

<interfaces>
<!-- Key facts from research about README targets -->

From research (direct grep of ~/Downloads/memo-skill/README.md):
- Line 189-190 contain: JobHunter.no, Finn.no, Webcruiter, Jobbnorge
- SKILL.md line 470 already has `my-saas-project` (generic) — no change needed there

Replacement mapping (per D-05):
- JobHunter.no → my-saas-project
- Finn.no → generic-jobs-api
- Webcruiter → careers-api
- Jobbnorge → jobboard-api

D-06: Preserve the structure and educational value of examples. Only the identifying names change.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace real project names in README examples (SEC-03, D-05, D-06)</name>
  <read_first>
    - ~/Projects/claude-memo/README.md (read lines 180-210 to see current example content before modifying)
  </read_first>
  <files>README.md</files>
  <action>
    Replace the four real project/company names with generic alternatives. Run a single sed command with all replacements:

    ```bash
    sed -i '' \
      's|JobHunter\.no|my-saas-project|g;
       s|Finn\.no|generic-jobs-api|g;
       s|Webcruiter|careers-api|g;
       s|Jobbnorge|jobboard-api|g' \
      ~/Projects/claude-memo/README.md
    ```

    Note: `JobHunter.no` uses `\.` to escape the dot (literal period in sed).

    After running, display the changed section to verify the replacements look natural in context:
    ```bash
    grep -n "my-saas-project\|generic-jobs-api\|careers-api\|jobboard-api" ~/Projects/claude-memo/README.md
    ```

    D-06: Do NOT restructure the examples, remove explanatory text, or change any surrounding prose. Only the company/project name strings change.
  </action>
  <verify>
    <automated>
      grep -n "JobHunter\|Finn\.no\|Webcruiter\|Jobbnorge" ~/Projects/claude-memo/README.md && echo "FAIL — real names remain" || echo "PASS — zero real names"
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "JobHunter" ~/Projects/claude-memo/README.md` exits 1 (no matches)
    - `grep -n "Finn\.no" ~/Projects/claude-memo/README.md` exits 1 (no matches)
    - `grep -n "Webcruiter" ~/Projects/claude-memo/README.md` exits 1 (no matches)
    - `grep -n "Jobbnorge" ~/Projects/claude-memo/README.md` exits 1 (no matches)
    - `grep -n "my-saas-project\|generic-jobs-api\|careers-api\|jobboard-api" ~/Projects/claude-memo/README.md` exits 0 and shows lines (replacements present)
    - README.md line count is unchanged (±2 lines) — no structural damage
  </acceptance_criteria>
  <done>All four real project/company names replaced with generic equivalents. README structure and educational content preserved.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human review of README examples section (D-08)</name>
  <what-built>
    Automated sed replaced JobHunter.no, Finn.no, Webcruiter, and Jobbnorge with generic names.
    The README is at: ~/Projects/claude-memo/README.md
  </what-built>
  <how-to-verify>
    1. Open `~/Projects/claude-memo/README.md` and navigate to the examples section (was around lines 185-200)
    2. Read the examples carefully. Look for:
       - Any remaining real company names (Norwegian job boards, employers, clients)
       - Any Norwegian geographic references (city names, postal codes)
       - Any project-specific terminology that could identify a real employer
       - Any URLs pointing to real internal services
       - Any usernames, email fragments, or personal handles
    3. The generic replacements should be:
       - my-saas-project (was JobHunter.no)
       - generic-jobs-api (was Finn.no)
       - careers-api (was Webcruiter)
       - jobboard-api (was Jobbnorge)
    4. Confirm the examples still make sense as educational content (D-06: preserve educational value)
  </how-to-verify>
  <resume-signal>Type "approved" if clean, or describe any remaining issues to fix</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README examples | Contains context that could identify employer or clients — must be generalized before public git history |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | README real project names | mitigate | sed replacement with 4 known patterns; human review checkpoint catches anything subtle |
| T-03-02 | Information Disclosure | Subtle context in prose | mitigate | Human visual review (Task 2 checkpoint) is explicit requirement per D-08 |
</threat_model>

<verification>
After Task 1 (automated):
- `grep -c "JobHunter\|Finn\.no\|Webcruiter\|Jobbnorge" ~/Projects/claude-memo/README.md` returns 0

After Task 2 (human):
- Human has confirmed no subtle personal references in examples section
</verification>

<success_criteria>
README.md has zero occurrences of the four real project names. Human has visually confirmed examples are clean. Ready for comprehensive scan in Plan 04.
</success_criteria>

<output>
After completion, create `.planning/phases/01-security-cleanliness/01-03-SUMMARY.md`
</output>
