---
phase: 01-security-cleanliness
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - .gitignore
  - LICENSE
  - scripts/auto_memo_hook.sh
  - scripts/auto_memo.py
  - scripts/compile_logs.py
  - scripts/init_vault.sh
  - scripts/memo_engine.py
  - scripts/memo_mcp_server.py
  - scripts/memo_utils.py
  - scripts/pre_compact_save.py
  - scripts/save_raw_log.py
  - scripts/session_context.py
  - scripts/setup_automation.sh
  - scripts/should_suggest_memo.py
  - references/ARCHITECTURE.md
  - references/AUTO_MEMO_SETUP.md
  - README.md
  - SKILL.md
autonomous: true
requirements:
  - SEC-02
  - SEC-04

must_haves:
  truths:
    - ".gitignore contains a .memo/ entry so vault runtime data can never be accidentally committed"
    - "Zero occurrences of 'engineering-brain' remain in any .py, .sh, .md, or .txt file"
    - "LICENSE no longer contains 'Sergei Arutiunian' or 'Digital Planet AS'"
    - "MEMO_VAULT_PATH environment variable references are unchanged — only the hardcoded path string was replaced"
  artifacts:
    - path: ".gitignore"
      provides: "Vault runtime exclusion"
      contains: ".memo/"
    - path: "LICENSE"
      provides: "Generic attribution — no personal name or company"
      contains: "claude-memo contributors"
    - path: "scripts/init_vault.sh"
      provides: "Vault initialization script with memo-vault default path"
  key_links:
    - from: "scripts/init_vault.sh"
      to: "~/memo-vault"
      via: "sed replacement of engineering-brain"
      pattern: "memo-vault"
    - from: ".gitignore"
      to: ".memo/"
      via: "appended entry"
      pattern: "\\.memo/"
---

<objective>
Apply three independent sanitization fixes to the copied files: (1) add `.memo/` exclusion to `.gitignore`, (2) bulk-replace all `engineering-brain` path references with `memo-vault`, and (3) update LICENSE to remove personal name and company.

Purpose: These are the mechanical, grep-verifiable fixes. No judgment calls — each has an exact target and a clear before/after.
Output: .gitignore has `.memo/` entry; zero `engineering-brain` occurrences; LICENSE has generic attribution.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-security-cleanliness/01-CONTEXT.md
@.planning/phases/01-security-cleanliness/01-RESEARCH.md
@.planning/phases/01-security-cleanliness/01-01-SUMMARY.md

<interfaces>
<!-- Key facts extracted from research about the files being modified -->

From ~/Downloads/memo-skill/.gitignore (current content — missing .memo/):
```
__pycache__/
*.pyc
*.pyo
.DS_Store
*.egg-info/
dist/
build/
```
.memo/ is NOT present — must be added.

From ~/Downloads/memo-skill/LICENSE (line 3):
```
Copyright (c) 2026 Sergei Arutiunian / Digital Planet AS
```
Target replacement: `Copyright (c) 2026 claude-memo contributors`

engineering-brain count: 35 occurrences across 13 files (README.md, SKILL.md, scripts/*.py, scripts/*.sh, references/AUTO_MEMO_SETUP.md).
The "9 occurrences" in REQUIREMENTS.md is an undercount — do NOT gate on 9. Gate on zero remaining.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add .memo/ entry to .gitignore (SEC-04)</name>
  <read_first>
    - ~/Projects/claude-memo/.gitignore (read full content before modifying)
  </read_first>
  <files>.gitignore</files>
  <action>
    Read the current `.gitignore` content first. Then append the `.memo/` exclusion block at the end of the file.

    ```bash
    printf '\n# Vault runtime data (never commit)\n.memo/\n' >> ~/Projects/claude-memo/.gitignore
    ```

    This adds a blank line for readability, a comment, and the `.memo/` entry.

    Verify immediately:
    ```bash
    grep ".memo" ~/Projects/claude-memo/.gitignore
    ```
    Must output `.memo/`.
  </action>
  <verify>
    <automated>grep ".memo/" ~/Projects/claude-memo/.gitignore || echo "MISSING"</automated>
  </verify>
  <acceptance_criteria>
    - `grep ".memo/" ~/Projects/claude-memo/.gitignore` exits 0 and prints `.memo/`
    - The rest of the .gitignore content is unchanged (existing entries __pycache__/, *.pyc, etc. still present)
    - `grep "__pycache__" ~/Projects/claude-memo/.gitignore` still exits 0
  </acceptance_criteria>
  <done>.gitignore contains `.memo/` exclusion. SEC-04 satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Bulk replace engineering-brain with memo-vault (SEC-02, D-03, D-04)</name>
  <read_first>
    - ~/Projects/claude-memo/scripts/init_vault.sh (contains vault path references — representative file)
    - ~/Projects/claude-memo/references/AUTO_MEMO_SETUP.md (contains vault path references)
  </read_first>
  <files>
    scripts/auto_memo_hook.sh
    scripts/auto_memo.py
    scripts/compile_logs.py
    scripts/init_vault.sh
    scripts/memo_engine.py
    scripts/memo_mcp_server.py
    scripts/memo_utils.py
    scripts/pre_compact_save.py
    scripts/save_raw_log.py
    scripts/session_context.py
    scripts/setup_automation.sh
    scripts/should_suggest_memo.py
    references/ARCHITECTURE.md
    references/AUTO_MEMO_SETUP.md
    README.md
    SKILL.md
  </files>
  <action>
    Run a single `find` + `sed` pass to replace every occurrence of `engineering-brain` with `memo-vault` across all text files in the repo, excluding `.planning/`.

    macOS (Darwin) requires `sed -i ''` (empty string after -i). This is the correct syntax for BSD sed.

    ```bash
    find ~/Projects/claude-memo \
      -not -path "*/\.planning/*" \
      -not -path "*/.git/*" \
      -type f \
      \( -name "*.py" -o -name "*.sh" -o -name "*.md" -o -name "*.txt" \) \
      -exec sed -i '' 's|engineering-brain|memo-vault|g' {} +
    ```

    This replaces all 35 occurrences (the REQUIREMENTS.md says "9" but research confirmed 35 actual occurrences — do NOT stop early).

    IMPORTANT: `MEMO_VAULT_PATH` references must remain unchanged. The sed pattern `engineering-brain` only matches the literal path string, not the environment variable name. Verify after:
    ```bash
    grep -rn "MEMO_VAULT_PATH" ~/Projects/claude-memo/scripts/ | head -5
    ```
    Must still show MEMO_VAULT_PATH references (per D-04).
  </action>
  <verify>
    <automated>
      grep -rn "engineering-brain" ~/Projects/claude-memo/ --include="*.py" --include="*.sh" --include="*.md" --include="*.txt" && echo "FAIL — occurrences remain" || echo "PASS — zero occurrences"
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "engineering-brain" ~/Projects/claude-memo/ --include="*.py" --include="*.sh" --include="*.md" --include="*.txt"` returns exit code 1 (no matches) — prints nothing
    - `grep -rn "memo-vault" ~/Projects/claude-memo/scripts/ | wc -l` returns a number greater than 0 (replacements took effect)
    - `grep -rn "MEMO_VAULT_PATH" ~/Projects/claude-memo/scripts/ | wc -l` returns a number greater than 0 (env var references intact, per D-04)
    - `.planning/` directory untouched: `grep -rn "engineering-brain" ~/Projects/claude-memo/.planning/` may still return results (that is fine — .planning/ contains research docs that mention the old name for reference)
  </acceptance_criteria>
  <done>Zero `engineering-brain` occurrences in any source file. All 35 occurrences replaced with `memo-vault`. MEMO_VAULT_PATH env var references preserved. SEC-02 satisfied.</done>
</task>

<task type="auto">
  <name>Task 3: Update LICENSE to generic attribution</name>
  <read_first>
    - ~/Projects/claude-memo/LICENSE (read full content — confirm line 3 has "Sergei Arutiunian / Digital Planet AS")
  </read_first>
  <files>LICENSE</files>
  <action>
    The LICENSE file contains on line 3:
    ```
    Copyright (c) 2026 Sergei Arutiunian / Digital Planet AS
    ```

    Replace it with:
    ```
    Copyright (c) 2026 claude-memo contributors
    ```

    Run:
    ```bash
    sed -i '' 's|Sergei Arutiunian / Digital Planet AS|claude-memo contributors|g' ~/Projects/claude-memo/LICENSE
    ```

    Note: The baseline scan pattern in MEMO_GITHUB_PUBLISH.md uses lowercase `sergeiarutiunian` but the LICENSE has proper case `Sergei Arutiunian` — this is NOT caught by the baseline scan. This task fixes it proactively so the comprehensive scan in Plan 04 returns clean.

    Verify:
    ```bash
    cat ~/Projects/claude-memo/LICENSE
    ```
    Line 3 must read: `Copyright (c) 2026 claude-memo contributors`
  </action>
  <verify>
    <automated>
      grep -i "Sergei Arutiunian" ~/Projects/claude-memo/LICENSE && echo "FAIL — name still present" || echo "PASS"
      grep "claude-memo contributors" ~/Projects/claude-memo/LICENSE || echo "FAIL — replacement not found"
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -i "Sergei Arutiunian" ~/Projects/claude-memo/LICENSE` exits 1 (no match)
    - `grep "claude-memo contributors" ~/Projects/claude-memo/LICENSE` exits 0 and prints the line
    - The rest of the LICENSE (MIT license body) is unchanged — `grep "Permission is hereby granted" ~/Projects/claude-memo/LICENSE` exits 0
  </acceptance_criteria>
  <done>LICENSE contains `claude-memo contributors` instead of personal name and company. MIT license body unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .gitignore coverage | If .memo/ is not excluded and user later points vault at repo directory, runtime data could enter git history |
| sed replacement scope | The find command must exclude .planning/ to avoid corrupting research documents |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | .gitignore missing .memo/ | mitigate | Task 1 appends .memo/ entry; verify with grep before proceeding |
| T-02-02 | Information Disclosure | engineering-brain in source | mitigate | Bulk sed with find covers all 35 occurrences; zero-result grep is acceptance gate |
| T-02-03 | Information Disclosure | Personal name in LICENSE | mitigate | sed replacement targets exact string; case-insensitive grep verifies removal |
| T-02-04 | Tampering | sed modifies .planning/ docs | mitigate | find excludes `*/\.planning/*` path — research documents mentioning the old name are preserved |
</threat_model>

<verification>
After all three tasks:
1. `grep ".memo/" ~/Projects/claude-memo/.gitignore` — exits 0
2. `grep -rn "engineering-brain" ~/Projects/claude-memo/ --include="*.py" --include="*.sh" --include="*.md" --include="*.txt"` — exits 1 (no output)
3. `grep -i "Sergei Arutiunian" ~/Projects/claude-memo/LICENSE` — exits 1 (no output)
4. `grep "MEMO_VAULT_PATH" ~/Projects/claude-memo/scripts/save_raw_log.py` — exits 0 (env var intact)
</verification>

<success_criteria>
.gitignore has .memo/ entry. Zero engineering-brain occurrences in source files. LICENSE has generic attribution. Ready for README sanitization in Plan 03.
</success_criteria>

<output>
After completion, create `.planning/phases/01-security-cleanliness/01-02-SUMMARY.md`
</output>
