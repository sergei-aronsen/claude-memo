# Core dependencies (ranges must match pyproject.toml)
sentence-transformers>=3.0,<4   # multilingual-e5-large embeddings
numpy>=1.26,<3                  # embedding storage and similarity search
PyYAML>=6.0                     # frontmatter parsing (safe fallback without it)

# MCP Server (optional — only needed if using memo_mcp_server.py)
mcp[cli]>=1.0                   # Model Context Protocol SDK
